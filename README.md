# todo-list-app

A simple to-do list app built with plain HTML, CSS, and JavaScript — no frameworks or dependencies.

## Usage

Open `index.html` in a browser. Type a task and press Enter (or click "Add Task") to add it to the list.
